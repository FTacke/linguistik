# Normalized Audio Output

This directory is reserved for review copies created by `audio_normalize.py`.

- Source files remain untouched under `docs/assets/audiofiles/`.
- Normalized files are written here with the same relative folder structure.
- Generated review copies can be deleted and recreated at any time.